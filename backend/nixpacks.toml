[phases.setup]
nixPkgs = ["nodejs_22", "openssl_3"]
aptPkgs = []

[phases.install]
cmds = ['npm install --include=dev']

[phases.build]
cmds = ['npx prisma generate', 'npm run build']

[start]
cmd = 'npm start'
